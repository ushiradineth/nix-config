# Flakes

Flakes are a way to define development environments. The files in these directory are example for
most of the languages that I use. Use this as a reference and extend on top of it.

- `helix.nix` is a single-file Helix workflow template. Run `hxw` after `nix develop`.
